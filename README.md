# REDLINE — Visual Designer Portfolio

一个使用 React + Vite 搭建的黑红系视觉设计师作品集基础版。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

当前图片与 Hero 视频为可替换的远程占位素材。主要页面内容位于 `src/App.jsx`，全局视觉样式位于 `src/styles.css`。
